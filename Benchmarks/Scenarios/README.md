# Benchmark Scenarios

This directory documents benchmark scenario sizes used by scripts in this folder.

- `10` dependencies
- `50` dependencies
- `100` dependencies
- `250` dependencies

The scripts generate temporary SwiftPM projects under `.build/generated-benchmarks/`
to avoid committing large generated source files.
