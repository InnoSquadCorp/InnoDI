import InnoDI

struct PlainChild {}

@DIContainer
struct ParentContainer {
    @Provide(.input)
    var first: Int

    @Provide(.input)
    var second: Int

    @SubContainer(scope: .shared)
    var child: PlainChild
}

@main
struct FixtureApp {
    static func main() {}
}
