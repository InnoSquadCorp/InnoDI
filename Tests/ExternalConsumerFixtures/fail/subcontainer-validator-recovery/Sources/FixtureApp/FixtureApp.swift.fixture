import InnoDI

struct PlainChild {}

@DIContainer
struct ParentContainer {
    @Input
    var first: Int

    @Input
    var second: Int

    @SubContainer(scope: .shared)
    var child: PlainChild
}

@main
struct FixtureApp {
    static func main() {}
}
