// swift-tools-version: 6.2

import PackageDescription

let package = Package(
    name: "GenerateMockModuleShadowConsumer",
    platforms: [.macOS(.v14)],
    dependencies: [
        .package(path: "{{INNODI_PACKAGE_PATH}}")
    ],
    targets: [
        .target(name: "ShadowKit"),
        .executableTarget(
            name: "FixtureApp",
            dependencies: [
                "ShadowKit",
                .product(
                    name: "InnoDI",
                    package: "{{INNODI_PACKAGE_IDENTITY}}"
                )
            ]
        )
    ]
)
