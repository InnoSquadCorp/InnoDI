// swift-tools-version: 6.2

import PackageDescription

let package = Package(
    name: "AssistedFactoryPrototypeConsumer",
    platforms: [
        .macOS(.v14)
    ],
    dependencies: [
        .package(path: "{{INNODI_PACKAGE_PATH}}")
    ],
    targets: [
        .target(
            name: "FeatureKit",
            dependencies: [
                .product(
                    name: "InnoDI",
                    package: "{{INNODI_PACKAGE_IDENTITY}}"
                )
            ]
        ),
        .executableTarget(
            name: "FixtureApp",
            dependencies: ["FeatureKit"]
        )
    ]
)
