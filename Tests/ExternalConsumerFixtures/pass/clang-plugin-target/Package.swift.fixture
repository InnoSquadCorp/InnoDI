// swift-tools-version: 6.2
import PackageDescription

let package = Package(
    name: "ClangPluginTargetConsumer",
    platforms: [.macOS(.v14)],
    dependencies: [.package(path: "{{INNODI_PACKAGE_PATH}}")],
    targets: [
        .target(
            name: "CProbe",
            plugins: [.plugin(name: "InnoDIDAGValidationPlugin", package: "{{INNODI_PACKAGE_IDENTITY}}")]
        ),
        .executableTarget(
            name: "FixtureApp",
            dependencies: [
                "CProbe",
                .product(name: "InnoDI", package: "{{INNODI_PACKAGE_IDENTITY}}"),
            ]
        ),
    ]
)
