name: Examples

permissions:
  contents: read

on:
  pull_request:
    paths:
      - 'Examples/**'
      - 'Sources/**'
      - 'Package.swift'
      - 'Package.resolved'
      - '.github/workflows/examples.yml'
  push:
    branches:
      - main
    paths:
      - 'Examples/**'
      - 'Sources/**'
      - 'Package.swift'
      - 'Package.resolved'
      - '.github/workflows/examples.yml'
  schedule:
    # Weekly Monday 06:00 UTC catches drift between Examples and Sources
    # even when no PR touches the Examples paths directly.
    - cron: '0 6 * * 1'
  workflow_dispatch:

jobs:
  sample-app:
    runs-on: macos-26
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Select Xcode 26.6
        uses: ./.github/actions/select-xcode
        with:
          version: "26.6"
      - name: Build and Test SampleApp
        run: |
          cd Examples/SampleApp
          swift build -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors
          swift test -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors
      - name: Run SampleApp
        run: |
          cd Examples/SampleApp
          swift run --skip-build SampleApp

  swiftui-example:
    # SampleApp is the representative PR consumer. The larger examples stay
    # exhaustive main/scheduled gates so a source edit does not trigger three
    # independent cold macro builds before review feedback is available.
    if: github.event_name != 'pull_request'
    runs-on: macos-26
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Select Xcode 26.6
        uses: ./.github/actions/select-xcode
        with:
          version: "26.6"
      - name: Build and Test SwiftUIExample
        run: |
          cd Examples/SwiftUIExample
          swift build -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors
          swift test -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors

  preview-injection-example:
    if: github.event_name != 'pull_request'
    runs-on: macos-26
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Select Xcode 26.6
        uses: ./.github/actions/select-xcode
        with:
          version: "26.6"
      - name: Build and Test PreviewInjectionExample
        run: |
          cd Examples/PreviewInjectionExample
          swift build -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors
          swift test -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors
