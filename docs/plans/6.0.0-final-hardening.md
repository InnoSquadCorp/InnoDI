# InnoDI 6.0.0 — final hardening

Status: implementation in progress; not release approval.
Baseline: `bd6412a98a7eec1c2e32f8d27c41139d8dedaf39`, PR #41.

## Decisions and boundaries

- R02 / D-H02: the user selected rejection of cyclic dependency graphs,
  including `Lazy` and `Provider`, on 2026-09-22. Deferred construction and
  detached handles remain supported for acyclic graphs. Weakening handles or
  adding an explicit scope-close obligation was not selected.
- Cycles retain resolver contexts even before first resolution. Reject them
  before generation, including with `validateDAG: false`; retain independent
  declaration-order and effect diagnostics. Test direct, deferred, mixed,
  self, and multi-node cycles, plus acyclic escaped-handle lifetime.
- Migration must preserve concurrent edits, not merely narrow the race with
  another pre-rename check. Conflict recovery must keep user bytes available.
  R01 uses atomic exchange with retained recovery entries, including on normal
  success. No automatic conflict overwrite or non-atomic fallback is permitted.
  Closing editors and reviewing/removing recovery files is explicit user work.
  R09 preserves POSIX modes; broader ACL/xattr preservation is not claimed.
- Retry must reject unsafe participation before changing any generation.
  R04 / D-H03 uses scope-owned reservations in stable provider-ID order across
  overlapping plans. Custom providers without the private transaction contract
  are rejected before mutation, rather than sequentially invoking arbitrary
  throwing resets. No global registry or unchecked concurrency is introduced.
- Mulbyul is the only consumer in scope, test-only in isolation. Do not change
  its source, dependency pins, project artifacts, or existing worktree state.
- No merge, tag, release, reduced gate, or toolchain-floor increase is approved.

## Sequence and evidence

Historical T24–T43 completion records remain historical, not proof for this
candidate. Each item below needs new regression evidence before completion.

| Order | Audit IDs | Work | Status |
| --- | --- | --- | --- |
| 1 | R02 | Reject deferred ownership cycles across macro and graph surfaces | Implemented; focused local gates passed |
| 2 | R01, R09 | Concurrent-save preservation and file mode preservation | Implemented; focused local gates passed |
| 3 | R03 | Release host closure captures on close | Implemented; focused local gates passed |
| 4 | R05, R04 | Uniform reset dispatch and transactional subgraph retry | Implemented; focused local gates passed |
| 5 | R06, R07 | Child binding queries and referential graph validation | Implemented; focused local gates passed |
| 6 | R08 | Saturated, overlapping trace workload and negative gate tests | Implemented; local workload and budgets passed |
| 7 | R11 | Source-compatible public API normalization | Implemented; compiler fixture and baseline passed |
| 8 | R10 | Candidate-bound release trace gate | Implemented; workflow and rejection contracts passed |
| 9 | — | Documentation, policy, full gates, exact-SHA Mulbyul verification | Local gates passed; consumer source break; remote gates and policy approval pending |

Local focused passes, full-suite passes, remote CI, consumer verification,
and publication are separate outcomes. Record unavailable or failed gates
without converting them into passes.

### R02 verification — 2026-09-23

Swift 6.4 / Xcode 27, strict concurrency and warnings-as-errors:

- Selected macro (185), runtime (11), graph CLI (20), core graph (16), and
  read-only Doctor (1) tests passed: 233 tests. Parameterized invocations are
  not counted as additional test definitions.
- The new external SwiftPM `deferred-ownership-cycle` fixture failed to
  compile with exactly the required ownership-cycle diagnostic, even with
  `validateDAG: false`. The fixture contract test passed.
- Six localized README structure checks, documentation links, and
  `git diff --check` passed.
- Removed four obsolete cycle-allowing expansion snapshots; their scenarios
  now assert diagnostics. They remain recoverable from Git history.
- This is not a full-suite, minimum-toolchain, sanitizer, consumer-product, or
  release pass. Those final-candidate gates remain pending.

### R01 / R09 verification — 2026-09-23

- Strict migration suite: 49 tests passed, including the fresh dependency
  consumer executable, 5 new deterministic publication/rollback regressions,
  path-boundary, BOM, and idempotence checks. Doctor apply/read-only: 2 passed.
- Rebuilt the selected 7 tests with process umask `077`; all passed, including
  schema-v3 recovery-path reporting. Repeated the unchanged publication tests
  under umask `002`: 5 passed. No global umask mutation inside parallel tests.
- Documentation links, six README structure checks, and diff whitespace passed.
- Recovery files intentionally remain until reviewed; success does not claim
  that an uncooperative editor can never write later through its old handle.
  Those writes remain recoverable, rather than silently unlinked by the tool.

### R03 verification — 2026-09-23

- Clear stored factory/close callbacks before publishing idle or awaiting
  cleanup. Late in-flight operations keep their generation-specific cleanup;
  a new start during close keeps its own retry/close callbacks.
- Strict SwiftUI host tests: 21 passed, including two new continuation/signal
  lifetime regressions, mounted redraws, late candidates, and concurrent close.
- A still-running user factory necessarily retains its own captures until it
  returns; this change does not claim to forcibly release executing closures.

### R04 / R05 verification — 2026-09-23

- Strict async runtime suites: 16 test definitions passed, including all 18
  concrete/existential/generic reset × lifecycle-state combinations and 20
  overlapping-plan cases. Existing 100-waiter failure/retry/close coverage passed.
- Cancellation before commit, close during a reserved retry, closed/running
  descendants, and custom nontransactional participants preserve the all-or-none
  generation boundary. Reservations are released on preflight failure/cancellation.
- Reviewed public API baseline delta: four `DIAsyncScope` methods become
  explicitly async and one error case is added; no unrelated symbols changed.
  R11's stronger source-compatibility gate is still pending.

### R06 / R07 verification — 2026-09-23

- Centralized canonical dependency IDs for cycle validation and queries. Child
  mounts/factories depend on their own parent bindings, without merging mounts
  of the same child type. Renamed/repeated factory labels remain supported.
- Graph query/JSON/contract and CLI suites: 156 test definitions passed, including 15
  malformed reference variants. Each invalid document also fails the actual
  CLI self-diff contract command, instead of appearing unchanged.

### R08 verification — 2026-09-23

- Separately prefilled 4,096-event rings; 64 paced rounds with four writers and
  one full-buffer snapshot each. All 64 snapshots overlapped measured writer
  intervals in each of five local samples (gate minimum: 8 per quarter).
- Existing budgets unchanged: disabled 57.92/210 ns/resolution, enabled
  435.12/600, saturated maximum 448.54/2,500, snapshot maximum 46.88/5,000,
  contended minimum 4,210.83/5,000 ns/event. These are local observations,
  not a CI baseline refresh or a consumer performance claim.
- Executable report contracts accept the valid fixture and reject 12 malformed
  workload variants plus all five over-budget variants.

### R11 verification — 2026-09-23

- API schema v4 retains default-argument presence independently of declaration
  formatting, internal parameter names, and default-expression rendering.
  Unknown parameter fragment shapes fail closed rather than silently dropping
  the contract. This does not claim to detect behavioral changes in defaults.
- Actual compiler graphs have the same symbol ID before/after removing a
  default, but normalize differently. The unchanged omitted-argument consumer
  compiles before and fails after. Initializer, subscript, tuple, closure,
  generic constraint, repeated label, and whitespace fixtures also pass.
- Baseline review: only schema/default-presence metadata changed, with the
  same 324 symbols across three public products.

### R10 verification — 2026-09-23

- Release Gate runs trace performance on the clean exact dispatch SHA before
  packaging. It uploads SHA-named diagnostics with compiler/workload provenance;
  staging retains its required dependency on successful Release Gate completion.
- Strict release/workload suites: 21 tests passed. Seven workflow mutations
  (missing command/dependency/artifact binding, wrong SHA, conditional skip,
  continue-on-error, and ignored failure) are rejected. Wrong-SHA script
  preflight fails before compilation. Missing/mismatched/dirty report provenance
  fails the executable validator. No release workflow was dispatched.

### Final validation checkpoint — 2026-09-23

Runtime/source candidate `7a4752db155db2004bed08274bf07882290671e4`:

- Strict Swift 6.4 tests, partitioned to isolate cold consumers: 1,169 tests,
  one mechanical fix-it cold build, 12 external-consumer contract definitions
  (including their compile-pass/fail fixture matrices), and eight strict build
  integrations passed: **1,190 total test definitions**. The former fix-it
  timeout reproduced as a successful 117.9-second cold build this time.
- All 21 marked documentation snippets, DocC generation, global DAG, local
  documentation links, six README structure checks, action pins, validation
  opt-out checks, macro fatal-error guard, and privacy manifests passed.
- Clean SHA-bound trace report passed the unchanged budgets. A follow-up adds
  observed event totals to every snapshot, verifies each round's progress, and
  rejects non-integer counts/non-numeric costs. Its local dirty-tree report is
  explicitly not substituted for clean candidate evidence.
- Remote Xcode 26.6 in-process tests, documentation contracts, Apple platform
  builds, and renamed-checkout checks passed. Its API stage flagged eleven
  macros only, including parameterless macros. The normalizer now derives
  defaults even when the older macro graph omits `functionSignature`, with
  regression fixtures for both shapes and no baseline change. A remote rerun
  is required to confirm this compatibility fix.
- Mulbyul committed source `c17064f2be65a2226bd099198d6b654636ef48da` was cloned
  separately. Exact InnoDI SHA resolution and byte-identical package sources
  were checked. Tuist 4.203.4 installation/generation passed after creating
  ignored generated-source directories in the clone. The actual `Layers`
  iOS Simulator build failed at `DomainContainer.swift:5`, the expected removed
  `@Provide(.input)` API. No migration was applied. This is a confirmed consumer
  adoption blocker, not a consumer build pass. Preliminary use of a nonexistent
  SwiftPM `Layers` target and the wrong Tuist version are not product findings.
- Read-only Mulbyul Doctor completed: schema 3, 460 Swift files, one proposed
  path, ten error diagnostics, zero applied paths, zero recovery paths. The
  original working tree's 53 status entries, tracked-diff digest, and untracked
  file hashes remained unchanged. Original uncommitted changes were excluded
  from the committed-source consumer test and preserved.
- SECURITY.md still says 4.x while the verified public stable is 5.1.0.
  The security-policy skill requires exact-diff approval; that one-line 5.x
  correction was requested separately and has not been applied without it.
- Swift 6.2, sanitizers, exhaustive coverage/performance, and final remote CI
  are separate remaining gates. No merge, tag, or release publication occurred.
