# InnoDI 6.0.0 — final hardening

Status: implementation in progress; not release approval.
Baseline: `bd6412a98a7eec1c2e32f8d27c41139d8dedaf39`, PR #41.

## Decisions and boundaries

- R02 / D-H02: the user selected rejection of cyclic dependency graphs,
  including `Lazy` and `Provider`, on 2026-09-22. Deferred construction and
  detached handles remain supported for acyclic graphs. Weakening handles or
  adding an explicit scope-close obligation was not selected.
- Cycles retain resolver contexts even before first resolution. Reject them
  before generation, including with `validateDAG: false`; retain independent
  declaration-order and effect diagnostics. Test direct, deferred, mixed,
  self, and multi-node cycles, plus acyclic escaped-handle lifetime.
- Migration must preserve concurrent edits, not merely narrow the race with
  another pre-rename check. Conflict recovery must keep user bytes available.
- Retry must reject unsafe participation before changing any generation.
- Mulbyul is the only consumer in scope, test-only in isolation. Do not change
  its source, dependency pins, project artifacts, or existing worktree state.
- No merge, tag, release, reduced gate, or toolchain-floor increase is approved.

## Sequence and evidence

Historical T24–T43 completion records remain historical, not proof for this
candidate. Each item below needs new regression evidence before completion.

| Order | Audit IDs | Work | Status |
| --- | --- | --- | --- |
| 1 | R02 | Reject deferred ownership cycles across macro and graph surfaces | Implemented; focused local gates passed |
| 2 | R01, R09 | Concurrent-save preservation and file mode preservation | Pending |
| 3 | R03 | Release host closure captures on close | Pending |
| 4 | R05, R04 | Uniform reset dispatch and transactional subgraph retry | Pending |
| 5 | R06, R07 | Child binding queries and referential graph validation | Pending |
| 6 | R08 | Saturated, overlapping trace workload and negative gate tests | Pending |
| 7 | R11 | Source-compatible public API normalization | Pending |
| 8 | R10 | Candidate-bound release trace gate | Pending |
| 9 | — | Documentation, policy, full gates, exact-SHA Mulbyul verification | Pending |

Local focused passes, full-suite passes, remote CI, consumer verification,
and publication are separate outcomes. Record unavailable or failed gates
without converting them into passes.

### R02 verification — 2026-09-23

Swift 6.4 / Xcode 27, strict concurrency and warnings-as-errors:

- Selected macro (185), runtime (11), graph CLI (20), core graph (16), and
  read-only Doctor (1) tests passed: 233 tests. Parameterized invocations are
  not counted as additional test definitions.
- The new external SwiftPM `deferred-ownership-cycle` fixture failed to
  compile with exactly the required ownership-cycle diagnostic, even with
  `validateDAG: false`. The fixture contract test passed.
- Six localized README structure checks, documentation links, and
  `git diff --check` passed.
- Removed four obsolete cycle-allowing expansion snapshots; their scenarios
  now assert diagnostics. They remain recoverable from Git history.
- This is not a full-suite, minimum-toolchain, sanitizer, consumer-product, or
  release pass. Those final-candidate gates remain pending.
