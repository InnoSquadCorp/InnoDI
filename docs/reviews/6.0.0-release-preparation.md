# InnoDI 6.0.0 배포 준비 — 2026-09-24

> 이전 준비 시점의 기록이다. 이후 수정·최신 검증·별도 제품 제외 범위는
> [라이브러리 hardening 기록](6.0.0-library-hardening.md)을 따른다.

- 상태: **Draft — 로컬 보완 검증, 최종 원격 gate와 정책 승인 대기**
- 승인자 / 승인일: 정책·RFC 승인 미기록. 사용자 요청 “그럼 6.0.0 배포 준비를 마무리 해”에 따른 준비 기록이며 공개 배포 승인 기록은 아니다.
- 선행 계획: [T56–T65](../plans/6.0.0-comprehensive-review-follow-up.md). 이전 전체 검토: [2026-09-23 수정 검증](6.0.0-comprehensive-follow-up.md).
- 작업본: `codex/rfc-0006-promotion`, HEAD `c7c51aabba9cec0f9ef701b9dd4d54fd2a8bad74` + 미커밋 수정. HEAD를 수정본 SHA로 사용하지 않는다.
- 환경: Xcode 27.0 (27A266a), Swift 6.4 (`swiftlang-6.4.0.34.1`), SwiftSyntax 603.0.2. 로컬 Swift 6.2/6.3.3 도구체인 없음.
- 새 증거 디렉터리: `/Users/changwooson/Documents/Codex/2026-09-24/innodi-release-preparation/`. 아래 로그명은 여기에 상대적이다.
- 코드/CI 622개 파일의 `code-ci-manifest.sha256` digest: `2f6f9b9f7dbe7e2296f7cd93a9449f786a901fb5eeb650014ee2adc15df4b092`. Sources/Tests/Tools/Plugins/Examples/Package 파일과 `.github`를 포함한다. 문서-only 편집은 별도 Git diff로 추적한다.

## 이번 보완과 fresh 증거

| 범위 | 결과 | 증거 |
|---|---|---|
| SampleApp checkout identity | 고정 `InnoDI` dependency/plugin identity를 다른 예제와 같은 basename 정규화로 교체. 실제 `candidate` 경로에서 strict build/test/run 통과 | `sample-renamed-checkout.log`: 1 test, 실행 결과 출력 |
| cache 관측 | `metadata-hit` 설명에 fresh content hash 확인을 명시. 일부 hit/miss 혼합을 전체 hit라고 표현하지 않음 | `final-polish-contracts.log`: 관련 coordinator/summary/manifest/CI 92 tests, 4 suites 통과 |
| CI 보완 | 기존 renamed-checkout lane에 SampleApp strict test/run 추가, 회귀 assertion 추가 | 위 92개 중 CI/manifest 계약. 원격에서 실행됐다는 의미는 아님 |
| release 계약 | candidate metadata, release workflow, deterministic packaging, clean-SHA trace, macro report/budget script 계약 재실행 | `release-contracts.log`: 73 tests, 6 suites 통과 |
| public API | 현재 작업본 API gate 재실행, 공개 baseline 추가 변경 없음 | `public-api.log`: 324 symbols / 3 graphs 통과 |
| 문서 packaging rehearsal | 이전 생성 DocC를 두 번 포장해 byte 동일 확인 | `docc-first.tar.gz`, `docc-second.tar.gz`: SHA-256 `59c6ceb271aae83f51a46cd82ff22aea5c64b966e12a7b7e8a63c5cd22bd23b5` |
| 정적 계약 | macro fatalError 금지, CI opt-out 금지, action pin 39개/7 workflows, README 6개 번역 정합, privacy manifest 2개, whitespace | 이번 명령 실행 exit0; `doc-links.log`: 16개 문서의 110 local links 통과 |

DocC archive는 공개 release asset이 아니라 재현성 rehearsal이다. 그 문서 입력은 9월 23일 생성 결과를 재사용했다. 전체 1,206 tests/103 suites, runtime 깊이·수명·process-boundary probes, DocC/snippets, SwiftUI 예제 8개와 Mulbyul build/Doctor는 **9월 23일 증거**이며 이번에 모두 재실행했다고 주장하지 않는다. 이번 runtime/공개 API 구현 변경은 없고 추가 변경은 위 예제·설명·CI 계약과 문서다.

## GitHub 배포 보호 설정 — read-only fresh 확인

GitHub CLI의 InnoSquad 계정 `Ethan-IS`와 repo ADMIN 접근, 실제 `github` remote를 확인했다. 로컬 `origin`은 원본 작업 저장소를 가리키므로 원격 게시 대상으로 사용하지 않는다.

- [PR 41](https://github.com/InnoSquadCorp/InnoDI/pull/41): OPEN/Draft, head `c7c51aabba9cec0f9ef701b9dd4d54fd2a8bad74`, base `82c9074aed63a083a09961995bd867cc45ad9925`, `release-validation` label 존재. fetch 후 HEAD와 해당 remote branch 차이 `0/0`.
- 공개 stable은 `5.1.0`; `6.0.0` 공개 배포는 없음. RELEASING/README의 stable metadata를 미리 바꾸지 않았다.
- immutable releases enabled; repository 공개 상태.
- `RELEASE_MAIN_RULESET_ID=15303479`: active, `refs/heads/main`, bypass/exclude 없음, deletion/non-fast-forward 차단, 정상 fast-forward 허용.
- `RELEASE_TAG_RULESET_ID=19127254`: active, `refs/tags/*`, bypass/exclude 없음, update/deletion 차단, 생성 허용.
- `release` environment: admin bypass 금지, required-reviewer rule 1개, 서로 다른 reviewer 2명, self-review 금지. custom deployment branch는 `main` 1개.
- environment secret `RELEASE_ADMIN_TOKEN` 등록 확인. **값·권한·만료·실제 workflow 인증 성공을 확인한 것은 아니다.** secret을 읽거나 바꾸지 않았다.
- 기존 c7 Exhaustive 실패와 다른 성공 check는 현재 dirty 수정본의 증거가 아니다. 수정본을 push하거나 workflow를 재실행하지 않았다.

## 남은 결정과 완료 gate

| 작업 | 남은 조건 | 현재 상태 |
|---|---|---|
| T62 / CR-FR08 | SECURITY `4.x` 문구를 최신 공개 stable major 기준으로 명확히 하는 exact diff | 사용자 승인 요청, 파일 무변경 |
| RFC 0006 | 구현된 assisted factory/roles/Input/multibinding 계약에 대한 maintainer acceptance; RFC와 index를 함께 `Accepted`로 갱신 | cooldown은 경과했지만 인간 승인 미기록. 상태 무변경 |
| T62–T63 / CR-FR06,07,09 | 검증된 변경 commit/push 후 PR 41의 exact candidate CI, Xcode 26.6/Swift 6.3.3 macro budget, coverage, 지원 compiler/API, 전체 sanitizer/platform, clean-SHA trace | 게시·CI 실행 확인 요청 중. baseline/threshold 완화 없음 |
| U02 | 기존 외부 compile-fail fixture의 structured plugin 진단 누락을 최종 게시 후보 CI에서 재관측 | 이전 frozen fresh-scratch 전체 PASS를 근본 원인 해결로 바꾸지 않음 |
| T64 / CR-FR10 | 최종 commit SHA에 연결한 Mulbyul test-only 확인 | 이번 앱 소스/소비자 재검증 없음. 9월 23일 `.input` compile 실패와 Doctor ownership 오류 유지 |
| T65 | 최종 SHA에 대한 M01–M24 증거·제한과 gate 결과 종합 | 이번 갱신은 M13/M19/M20/M21/M24에 해당. 나머지는 선행 보고서의 증거와 한계를 유지 |

정책 승인 요청의 exact diff:

```diff
-Current supported line: `4.x`
+The supported line is the major version of the latest published stable release.
+Unreleased development branches and prereleases do not establish a new supported line.
```

공개 배포를 실행할 때만 [RELEASING checklist](../../RELEASING.md)에 따라 별도 최종 metadata commit, main merge, SHA-bound Release Gate가 필요하다. 그 workflow는 실제 tag/release를 생성하므로 준비용 테스트처럼 dispatch하지 않는다. 여기서는 그 실행이나 보호 설정 변경을 하지 않았다.

## 원본 보존

- Mulbyul 원본 HEAD `c17064f2be65a2226bd099198d6b654636ef48da`; status 및 untracked SHA-256 목록이 baseline과 byte 동일. tracked diff SHA-256 `cc625379da366231f449f8136e0d0ed48363727b54d79f7f13c05e46fc77dc7a` 유지. 새 증거: `mulbyul-status.txt`, `mulbyul-untracked.sha256`.
- InnoDI 원본 HEAD `82c9074aed63a083a09961995bd867cc45ad9925`, 기존 untracked `Derived/`, `InnoDI.xcodeproj/` 유지.
- commit/push/merge/tag/release 없음. 성공한 push가 없으므로 post-push XcodeBuildMCP cleanup도 실행하지 않았다.

로컬 보완 검증과 배포 준비의 최종 완료를 구분한다. 현재는 최종 후보 CI·성능·정책 승인 gate가 남아 있으므로 **release-ready를 선언하지 않는다.**
