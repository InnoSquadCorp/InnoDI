---
date: 2026-09-05
product: InnoDI
feature: 6.0.0 excellence program
feature-id: F-EX600
status: Draft
owner: InnoSquad maintainer / user
reviewed-by: []
approved-by:
approved-at:
---

# InnoDI 6.0.0 — 여덟 가지 개선의 완료 계약

## 문서 상태와 범위

- 상태: **Draft**. 사용자는 여덟 개선 방향을 모두 작업할 계획을 요청했다. 아래 세부 계약과 기술 설계의 승인을 받은 것으로 간주하지 않는다.
- 기준: 2026-09-05 로컬 `820fdcb` (`fix: avoid Swift 6.2 enum macro crash`). 원격 최신 상태나 배포 상태를 증명하는 문서가 아니다.
- 기술 실행 계획: [6.0.0 excellence implementation plan](../plans/6.0.0-excellence.md).
- 기존 [RFC 0006](../rfcs/0006-assisted-subgraphs-and-container-roles.md)의 `FR-600-*` / `AC-600-*`를 유지한다. 이 문서는 추가 계약이며 기존 문서를 승인·완료 상태로 변경하지 않는다.
- 여덟 항목은 모두 Must 범위다. 구현상 어려움이 생겼다는 이유로 임의로 다음 버전으로 미루거나 조용히 축소하지 않는다. 제품 계약 변경은 사용자에게 근거와 대안을 제시해 결정한다.
- 정식 release/upgrade notes의 단일 출처는 계속 [RELEASING.md](../../RELEASING.md)다.

## 1. 근거와 의도

### 확인한 사실

| 근거 | 현재 관찰 | 확인 방법 / 소스 |
|---|---|---|
| E01 | assisted factory가 static 목록 뒤 assisted 목록 순서로 호출 인자를 생성한다. 선언 순서 변경과 함수 입력에서 컴파일 실패를 재현했다. | [AssistedFactoryMacro](../../Sources/InnoDIMacros/AssistedFactoryMacro.swift), 로컬 Swift 6.4 consumer typecheck |
| E02 | 동기 shared는 초기화 중 생성되고 비동기 shared는 Task로 보관된다. 첫 접근 전 생성 1회, 실패 후 두 번 읽어도 시도 1회, 새 컨테이너에서 다음 시도 성공을 재현했다. | [생성 코드](../../Sources/InnoDIMacros/DIContainerCodeGenerator.swift), 임시 runtime probe |
| E03 | `@GenerateMock`은 Sendable 상속과 MainActor isolation을 거부한다. | [GenerateMockMacro](../../Sources/InnoDIMacros/GenerateMockMacro.swift), 두 strict consumer typecheck |
| E04 | 멀티바인딩은 직접 동기 멤버의 written type 일치를 요구한다. 구체 구현체 배열 결합은 실패하고 existential 선언은 통과했다. | [공개 API](../../Sources/InnoDI/InnoDI.swift), 비교 typecheck |
| E05 | JSON v3에는 입력 타입, provider 수명, actor isolation이 없다. 이 세 종류 변경이 contract diff에서 모두 종료 코드 0이었다. | [JSONRenderer](../../Sources/InnoDIDependencyGraphCore/Rendering/JSONRenderer.swift), CLI 비교 |
| E06 | feature-root helper는 고정 subcontainer를 인자로 View를 생성한다. InnoSample People route는 assisted child 보관용 State 래퍼를 직접 작성한다. | [FeatureRootGenerator](../../Sources/InnoDIMacros/DIContainerFeatureRootGenerator.swift), InnoSample `Features/PeopleFeature/Router/Routes/PeopleRoute.swift` 소스 확인; 실제 UI 수명 재시험은 미실시 |
| E07 | SwiftUI environment bridge의 멤버 이름은 문자열이고, factory는 입력 분류를 별도 키패스 목록에 반복한다. | [InnoDISwiftUI](../../Sources/InnoDISwiftUI/InnoDISwiftUI.swift), E01 |
| E08 | migration check/report/write와 graph query/maintenance 명령은 있지만 이를 연결하는 doctor 흐름은 없다. migration report의 change 항목은 파일 경로와 일반 코드 중심이다. | [MigrationArguments](../../Sources/InnoDIMigrationCore/MigrationArguments.swift), [MigrationReport](../../Sources/InnoDIMigrationCore/MigrationReport.swift), [Graph Arguments](../../Sources/InnoDIDependencyGraphCLI/CLI/Arguments.swift) |

임시 probe는 영구 회귀 테스트가 아니다. 실행 계획 T00에서 저장소 fixture로 옮겨 최소 toolchain에서도 확인한다. E02–E04는 현재 명시된 기능 제약이며 모두 기존 동작의 버그라는 뜻은 아니다.

### 사용자와 성공 경험

| ID | 사용자 의도 |
|---|---|
| US-EX600-001 | 앱 개발자는 필요한 의존성을 필요한 시점에 생성하고, 화면·세션의 수명과 함께 안전하게 종료하고 싶다. |
| US-EX600-002 | 테스트 작성자는 실제 effect를 실행하지 않고 동시성·실패·프리뷰 상태를 반복 가능하게 검증하고 싶다. |
| US-EX600-003 | 유지보수자는 선언 중복 없이 기능을 연결하고, 잘못된 연결·느린 생성·업그레이드 영향을 소스 위치까지 추적하고 싶다. |

### 작업 가정

- Swift 6.2 최소 지원과 현재 Apple deployment floor를 유지하는 방향으로 먼저 설계한다. 변경이 필요하면 근거와 이주 비용을 별도로 승인받는다.
- 기존 eager shared 동작은 유지하고 on-demand 생성은 명시적 선택으로 추가한다.
- API 이름, 신규 product 이름, graph schema 번호는 기술 검증 전까지 후보일 뿐이다.
- InnoSample → Mulbyul Apple → BlPia Apple을 소비자 검증 순서로 삼되, 각 checkout/version/미커밋 변경과 legacy migration 선행 조건을 재확인한다.

### Non-goals

- 런타임 서비스 로케이터, reflection/module 자동 검색, 등록 순서에 의존하는 graph 구성.
- 이미 생성된 shared 객체의 내부 의존성을 TaskLocal override가 소급 변경하는 것처럼 보이게 하는 동작.
- 기존 live graph의 일부 캐시만 바꿔 서로 다른 세대의 의존성이 섞이는 재시도.
- actor/Sendable 진단을 끄거나 검증을 우회해 기능을 지원하는 방식.
- 임의 사용자 서비스가 내부적으로 만든 모든 작업을 강제로 취소한다고 보장하는 것.
- 이 계획 작성만으로 제품 코드 변경, 소비자 dependency upgrade, push, tag 또는 release publication을 실행하는 것.

## 2. 기능 요구사항

| ID | 항목 | 사용자에게 보장할 결과 | 우선순위 |
|---|---|---|---|
| FR-EX600-000 | 선행 결함 | assisted 입력의 선언 순서·escaping·키패스 소유 타입·접근 수준에 대해 올바른 코드 또는 원본 위치의 진단을 제공한다. graph contract의 현재 미감지 사례를 회귀로 고정한다. | Must |
| FR-EX600-001 | 1. 생성 시점과 수명 분리 | shared/transient 의미를 유지하면서 eager/on-demand 생성과 선택적 prewarm을 명시적으로 사용한다. 동일 논리 컨테이너의 shared 초기화는 정확히 한 번이다. | Must |
| FR-EX600-002 | 2. 비동기 생명주기 | 선택한 의존성의 준비 상태·실패 원인·취소 결과를 알 수 있고, 명시적 scope 종료 및 새 child 세대로 재시도할 수 있다. | Must |
| FR-EX600-003 | 3. SwiftUI 호스트 | fixed/assisted child를 화면·route·document·window identity에 따라 보관하고 생성·종료한다. root helper와 프리뷰까지 같은 계약을 사용한다. | Must |
| FR-EX600-004 | 4. 테스트와 프리뷰 | Sendable/MainActor mock, typed throws, 안전한 호출 기록, 누락 stub/예상 밖 호출 보고, 재사용 가능한 typed override preset을 제공한다. | Must |
| FR-EX600-005 | 5. 확장 멀티바인딩 | 구체→프로토콜 결합, 명시적 empty, keyed map, provider collection, 모듈 간 명시적 결합을 제공한다. 순서·키 충돌·수명은 결정적이다. | Must |
| FR-EX600-006 | 6. 의존성 디버거 | provider 단위 생성 이유·보유 관계·override 출처·대기 의존성·생성 횟수/시간을 소스와 연결한다. graph contract는 타입·수명·격리·구성 변경을 검출한다. | Must |
| FR-EX600-007 | 7. 선언 단일화 | 입력 분류와 타입의 단일 원천을 유지하고 factory bridge를 유도/생성한다. environment bridge는 키패스 기반으로 연결하며 누락 선언의 안전한 Fix-it을 제공한다. | Must |
| FR-EX600-008 | 8. doctor와 migration | 설치/target/plugin/toolchain 진단 → 구체적 수정안 → 명시적 적용 → 코드·graph 차이 및 consumer build 검증을 연결한다. 진단 기본값은 read-only다. | Must |

## 3. 수용 기준

아래 테스트 이름은 신규 테스트 산출물의 제안명이다. 아직 존재하거나 통과한 것으로 간주하지 않는다.

| ID | 요구사항 | Given / When | Then |
|---|---|---|---|
| AC-EX600-000 | FR-EX600-000 | static/assisted 입력을 섞거나 목록 순서를 바꾸고 직접/alias 함수 입력을 사용한다. 타 타입 키패스와 public/package 경계도 검사한다. | 유효한 소비자는 same-file, separate-file, cross-module 모두 컴파일하고 잘못된 연결은 안정적인 진단으로 거부한다. compiler crash가 없다. |
| AC-EX600-001 | FR-EX600-001 | on-demand shared를 만들고 접근하지 않는다. | 해당 factory 실행 횟수 0. 기존 eager fixture는 이전과 같은 시점에 생성된다. |
| AC-EX600-002 | FR-EX600-001 | 지원되는 isolation에서 동일 shared에 100개 동시 접근, 컨테이너 값 복사 후 접근, 독립 컨테이너 생성, transient 반복 접근을 한다. | 논리 컨테이너당 shared 생성 1회, 복사본은 같은 shared identity, 독립 생성은 별도 identity, transient는 접근마다 새 값이다. |
| AC-EX600-003 | FR-EX600-001 | 일부 provider를 prewarm하며 override와 lazy/provider 참조를 함께 사용한다. | 선택된 전이 의존성만 준비한다. override한 factory는 실행하지 않는다. 재진입/순환은 무한 대기하지 않고 정의된 진단/실패를 낸다. |
| AC-EX600-004 | FR-EX600-002 | 성공·실패·아직 준비 중인 provider가 섞인 선택적 prepare를 실행한다. | 선택 집합의 상태와 실패 provider 경로/원본 오류를 구분한다. 미선택 provider 때문에 준비가 멈추지 않는다. |
| AC-EX600-005 | FR-EX600-002 | 같은 shared 작업의 대기자 둘 중 하나를 취소한 뒤 owner scope를 종료한다. | 먼저 취소된 대기자는 CancellationError로 대기를 종료하되 다른 대기자는 영향을 받지 않는다. owner 종료는 자신이 소유한 InnoDI 작업을 취소하고 새 작업 시작을 거부한다. 종료는 멱등적이다. |
| AC-EX600-006 | FR-EX600-002 | 생성 실패 후 재시도한다. | 새 child generation에서 실패 의존성과 그 downstream을 일관되게 재생성한다. 이전 child와 캐시가 섞이지 않으며 명시적으로 상속한 부모 의존성만 공유한다. |
| AC-EX600-007 | FR-EX600-003 | 같은 화면 identity로 body 갱신을 100회 유발한다. | child와 shared factory 실행은 1회. 버려질 임시 child도 생성하지 않는다. |
| AC-EX600-008 | FR-EX600-003 | identity 교체, 같은 payload의 독립 window/route, 닫기·재진입을 수행한다. | 각 owner는 독립 child를 갖는다. 교체/종료된 owner의 InnoDI 작업이 종료되고 다른 소유자가 없는 child-local 객체가 해제된다. 부모나 외부 소유자의 참조까지 해제한다고 보장하지 않으며 단순 onDisappear를 영구 종료와 혼동하지 않는다. |
| AC-EX600-009 | FR-EX600-003 | fixed/assisted root helper와 preview를 사용한다. | 추가 수동 State 래퍼 없이 동일한 ownership 및 MainActor 계약이 적용된다. |
| AC-EX600-010 | FR-EX600-004 | Sendable/MainActor/public/package 프로토콜과 throws(E) 요구를 mock으로 만든다. | 올바른 isolation, 접근 수준, 오류 타입을 보존해 strict consumer가 컴파일한다. 지원 불가능한 조합은 명시적 진단을 낸다. |
| AC-EX600-011 | FR-EX600-004 | 100개 동시 호출과 snapshot/reset, 누락 stub, 예상하지 않은 호출을 검사한다. | 기록 유실/data race 없이 확인한다. 정상적인 설정 검증 실패는 테스트 실패로 보고되며 live 구현으로 fallback하지 않는다. typed throws에 임의 오류 타입을 끼워 넣지 않는다. |
| AC-EX600-012 | FR-EX600-004 | 재사용 preset을 결합하고 서로 다른 병렬 테스트/프리뷰에서 사용한다. | override 우선순위가 명시되고 컨텍스트가 누출되지 않는다. opt-in strict profile에서 미설정 effect 의존성을 consumer 코드 실행 전에 차단한다. |
| AC-EX600-013 | FR-EX600-005 | 구체 구현체를 existential 배열로 합치고 명시적 empty를 선언한다. | 유효한 변환은 컴파일하며 실제 불일치는 원본 contributor 위치에서 실패한다. empty와 잘못된 구문은 구별된다. |
| AC-EX600-014 | FR-EX600-005 | keyed map과 provider collection을 구성하고 일부 항목만 선택한다. | 키 중복을 검출한다. 순서/키 identity가 결정적이고 provider 선택만으로 미선택 항목을 생성하지 않는다. 각 contributor의 명시적 lifetime/initialization 계약을 보존한다. |
| AC-EX600-015 | FR-EX600-005 | 별도 모듈 컬렉션을 명시적으로 합성한다. | 선언한 import/결합 순서만 반영한다. nested 병합의 중복 키도 거부하고 module 검색이나 등록 순서에 의존하지 않는다. binary module 소비 경계도 정의한다. |
| AC-EX600-016 | FR-EX600-006 | 입력 타입·provider 수명·생성 정책·actor isolation·컬렉션 구성/순서를 변경한다. | contract gate가 검출한다. 단순 줄 이동이나 렌더링 순서 변화는 의미 변경으로 잘못 분류하지 않는다. 미지원/잘못된 schema는 unchanged로 통과하지 않는다. |
| AC-EX600-017 | FR-EX600-006 | container.member에 why/dependents를 조회하고 override/async 대기 경로를 확인한다. | provider까지 경로와 원본 source location을 제공한다. 모호한 selector에는 후보를 보여 준다. 관찰 가능한 보유 관계와 임의 객체 내부 참조를 구분한다. |
| AC-EX600-018 | FR-EX600-006 | trace를 켜고 반복 생성/느린 생성/취소를 실행한 뒤 끈다. | stable provider ID와 runtime instance를 연결한다. 입력값·토큰·사용자 데이터는 기록하지 않는다. buffer는 bounded이고 비활성 시 이벤트 기록/버퍼 할당이 없다. |
| AC-EX600-019 | FR-EX600-007 | 입력 추가·삭제·이름 변경·분류 변경을 수행한다. | 하나의 입력 선언에서 factory 계약이 반영된다. bridge 생성 방식이면 check가 stale 선언을 검출하고 생성은 idempotent다. |
| AC-EX600-020 | FR-EX600-007 | SwiftUI bridge 멤버 rename, 타 타입 키패스, chained key path를 사용한다. | 유효한 rename은 안전하게 반영되고 잘못된 root/타입/경로는 거부된다. 구형 spelling에는 명시적 migration 경로가 있다. |
| AC-EX600-021 | FR-EX600-007 | assisted bridge 또는 binding이 누락되어 Fix-it을 적용한다. | 명확한 경우만 수정한다. 적용 후 컴파일/검증에 성공하고 두 번째 실행의 diff는 0이다. 모호하면 후보와 이유를 반환하고 수정하지 않는다. |
| AC-EX600-022 | FR-EX600-008 | plugin 누락, visibility 문제, toolchain 불일치, plugin 분석이 불완전한 target을 doctor로 검사한다. | 진단 코드·위치·권고·분석 범위를 출력한다. 미분석 대상을 healthy로 표시하지 않고 source/config를 변경하지 않는다. |
| AC-EX600-023 | FR-EX600-008 | 명시적으로 수정안을 적용하되 stale patch·symlink·중첩 Git repo·dirty file·중도 실패를 섞는다. | 범위 밖 쓰기를 거부하고 충돌한 사용자 변경을 덮어쓰지 않는다. 실패한 적용을 복구하며 일반 Git commit/push는 하지 않는다. |
| AC-EX600-024 | FR-EX600-008 | migration을 진단→patch 검토→적용→검증으로 실행한다. | 코드 diff, graph 의미 diff, consumer build 결과를 함께 보고한다. 텍스트 변경 완료와 빌드 성공을 구분하고 반복 실행의 diff는 0이다. |

## 4. 공통 품질 계약

| ID | 제약 | 검증 |
|---|---|---|
| NFR-EX600-001 | macro/build-plugin/graph가 같은 의미 모델을 사용한다. Swift 타입 검사가 필요한 부분을 문자열 비교나 성공으로 위장하지 않는다. | 공통 semantic fixture + 컴파일러의 typed witness + 정확한 분석 경계 |
| NFR-EX600-002 | Swift 6.2 최소 toolchain과 현재 platform floor를 승인 없이 올리지 않는다. | 6.2/6.4 strict 외부 소비자, 현재 CI의 Apple platform matrix |
| NFR-EX600-003 | concurrency 우회, 검증 opt-out, coverage/performance floor 완화로 통과하지 않는다. | 기존 gates 유지, 적용 가능한 TSan/ASan, cancellation/race stress |
| NFR-EX600-004 | 기능을 끈 사용자의 비용과 기본 eager/override 동작을 보존한다. | baseline consumer, macro/cold/no-op/runtime 비교, generated API diff |
| NFR-EX600-005 | 디버그 trace와 테스트 지원의 의존성을 production runtime에 강제로 추가하지 않는다. | product dependency graph, release binary/trace-disabled 검사 |
| NFR-EX600-006 | source의 arbitrary object 참조·외부 작업 수명까지 분석/취소한다고 과장하지 않는다. | 명시적 metadata 기반 관찰 범위, unsupported/unknown 표기 |
| NFR-EX600-007 | 새 ABI/API/schema 의미는 명시적 migration·release note와 함께 관리한다. | public API 및 schema fixture, README/DocC/ROADMAP/RELEASING 동기화 |

## 5. 성공 기준

| ID | 기준 | 현재 baseline | 완료 목표 |
|---|---|---|---|
| SC-EX600-001 | 필수 계약과 조합 검증 | 임시 probe 및 기존 개별 기능 fixture | AC 000–024와 실행 계획의 조합 matrix 전부 통과; 실제 test/evidence 링크 존재 |
| SC-EX600-002 | 생성/수명 정확성 | eager 1회, on-demand/host 공식 계약 없음 | AC 001–009의 생성 횟수·identity·취소·해제 수치 만족 |
| SC-EX600-003 | 개발 경험 | 반복 factory 목록과 State 래퍼, 분리된 진단 명령 | 세 소비자 pilot에서 중복 입력 목록/수명 래퍼 제거 및 doctor→build 흐름 증명 |
| SC-EX600-004 | 비용 | checked-in macro/coverage gate와 기존 cold-build 계측 존재; 새 runtime baseline은 미측정 | 기존 임계값 유지. T02에서 동일 hardware 기준 신규 runtime/trace budget을 계측·검토해 고정 |
| SC-EX600-005 | 배포 신뢰 | local candidate; 이번 계획에서 원격 CI/정식 태그 재조회 안 함 | 최종 동일 candidate SHA의 모든 gate와 pilot evidence, 승인된 공식 Release Gate 사용 |

SC 측정 책임은 실행 담당자, 제품/API 의미 변경 및 비용 허용치의 결정 책임은 사용자/maintainer다. 측정이 없는 성능 목표를 이미 달성한 값처럼 기록하지 않는다.

## 6. 추적표

| 요구사항 | 수용 기준 | 실행 작업 | 예정 증거 | 상태 |
|---|---|---|---|---|
| FR-EX600-000 | AC-EX600-000, AC-EX600-016 | T00, T01, T17 | assisted regression + contract-drift fixture | assisted implemented; graph drift pending |
| FR-EX600-001 | AC-EX600-001, AC-EX600-002, AC-EX600-003 | T02, T03, T04, T05 | ConstructionPolicyContracts | planned, not implemented |
| FR-EX600-002 | AC-EX600-004, AC-EX600-005, AC-EX600-006 | T03, T06, T07 | AsyncScopeContracts | planned, not implemented |
| FR-EX600-003 | AC-EX600-007, AC-EX600-008, AC-EX600-009 | T08, T09, T10 | ContainerHostLifecycleContracts + UI pilot | planned, not implemented |
| FR-EX600-004 | AC-EX600-010, AC-EX600-011, AC-EX600-012 | T11, T12, T13 | ConcurrentMockContracts + PresetIsolationContracts | planned, not implemented |
| FR-EX600-005 | AC-EX600-013, AC-EX600-014, AC-EX600-015 | T14, T15, T16 | CollectionCompositionContracts | planned, not implemented |
| FR-EX600-006 | AC-EX600-016, AC-EX600-017, AC-EX600-018 | T03, T17, T18, T19 | ProviderGraphContracts + RuntimeTraceContracts | planned, not implemented |
| FR-EX600-007 | AC-EX600-019, AC-EX600-020, AC-EX600-021 | T02, T08, T21 | DeclarationDerivationContracts + FixItContracts | planned, not implemented |
| FR-EX600-008 | AC-EX600-022, AC-EX600-023, AC-EX600-024 | T20, T21, T22 | DoctorSafetyContracts + MigrationWorkflowContracts | planned, not implemented |
| NFR-EX600-001, NFR-EX600-002, NFR-EX600-003 | SC-EX600-001 | T00, T02, T03, T23 | shared semantics, minimum toolchain, sanitizer/gates | planned, not implemented |
| NFR-EX600-004, NFR-EX600-005, NFR-EX600-006 | SC-EX600-004 | T02, T13, T19, T23 | cost/dependency/trace boundary evidence | planned, not implemented |
| NFR-EX600-007 | SC-EX600-005 | T17, T21, T23 | API/schema migration and release gate | planned, not implemented |

## 7. 미결정 사항과 검토 게이트

| ID | 결정 | 권고 / 확인할 사항 | 책임 / 결정 시점 |
|---|---|---|---|
| D01 | 생성 정책 API와 shared 복사 semantics | 기본 eager 유지, 복사본은 같은 논리 cache owner 공유; 구체 spelling은 compiler spike 뒤 선택 | maintainer / T02 |
| D02 | async owner·cancel·retry API | 대기자와 owner 취소 분리, 새 child generation으로 재시도; 임의 외부 작업은 제외 | maintainer / T02 |
| D03 | 자동 factory bridge | source-visible nested type 유지. outer member-attribute가 내부 생성 macro로 ordered/escaping/actor metadata를 전달하며 public macro는 원본 진단을 소유. Swift 6.4 consumer 검증 완료, Swift 6.2 matrix pending | maintainer / T02 |
| D04 | schema compatibility | schema v4로 확정. v3 및 알 수 없는 버전은 diff에서 명시적으로 거부하며 baseline을 같은 도구로 재생성한다. source 줄/열은 진단 metadata로 직렬화하지만 의미 diff에서는 제외한다. | maintainer / T03 완료 |
| D05 | collection type/모듈 검증 | 구조는 parser, assignability는 compiler가 확인. binary-module collection contract의 전달 형식을 검증 | maintainer / T14 이전 |
| D06 | 테스트 product와 typed missing stub | 기존 내부 InnoDITestSupport target과 구분. typed error를 위조하지 않고 사전 stub 검증으로 실행을 차단 | maintainer / T11 이전 |
| D07 | 성능·trace budget | 기존 gate 유지, 새 동작은 baseline 측정 뒤 수치 결정. 총기간도 T02 이후 재산정 | maintainer / T02 |
| D08 | 소비자 migration 범위 | BlPia legacy 선행 migration과 앱별 수정 권한/dirty worktree를 재확인 | maintainer / T23 이전 |

검토 시 다음 반례 구현을 명시적으로 탈락시켜야 한다.

- on-demand라고 이름 붙였지만 dependency를 init에서 미리 만드는 구현.
- 취소된 대기자 때문에 모든 사용자의 shared 작업을 중단하는 구현.
- State 값은 유지하면서 View init마다 버려질 child를 새로 만드는 구현.
- Sendable mock이라는 이유로 검증되지 않은 unchecked conformance만 붙이는 구현.
- 서로 다른 모듈의 map 키 충돌을 런타임 last-wins로 숨기는 구현.
- debugger가 schema에 없는 정보를 unchanged/healthy로 처리하거나 opaque factory의 내부 참조를 안다고 표시하는 구현.
- doctor가 검사 중 파일을 수정하거나 migration patch가 사용자 변경을 덮는 구현.

- [ ] D01–D08의 담당 결정 및 반례 검토 결과 기록.
- [ ] 모든 FR/AC/NFR의 추적 및 실패·취소·복구 계약 검토.
- [ ] 실제 reviewer와 review date 기록 후에만 Reviewed로 변경.
- [ ] 사용자/maintainer 승인 기록 후에만 Approved로 변경.
- [ ] 각 구현 작업 완료 시 예정 test 이름을 실제 파일·case·run evidence로 교체.
